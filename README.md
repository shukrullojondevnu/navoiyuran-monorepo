# For create preconfigured new application

in root folder

```
npx turbo gen workspace --name=<name of new app> --copy=starter --type=app
```

# For add Shadcn component

enter "packages/ui" folder

```
npx shadcn@latest add <component name>
```
